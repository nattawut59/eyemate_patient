// utils/dateTimeHelper.js
const formatThaiDate = (date) => {
  // แปลงเป็นรูปแบบภาษาไทย
};

const formatTimeSlot = (time) => {
  // เช้า/บ่าย/เย็น
};

module.exports = { formatThaiDate, formatTimeSlot };